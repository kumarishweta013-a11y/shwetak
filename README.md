# shwetak
This is my first git repository -Shweta kumari
--Configuring git--
open git bash from window machine and then run below cmd

git config --global user.name "github account name here"
git config --global user.email "emaiid here"
git config list

--to use git from vscode---

ls //to check list of files
ls -a // to check hidden files
git clone <some link>// to copy github into vs code local machine--use https link to clone from git hub a/c
git status //to check status of the repsoitory Modified/Untracked/staged/unmodified

git add . //to add all modifed files .newly created files or any changed file
git commit -m "any comment" // to commit the changes
git push origin main // push changed file to github from local